<ul class=" col-xs-12 col-sm-10 col-md-6 mx-auto in-line">
    {% for cat in category_list %}
        <li><a href="{{ path('category_post_list',{slug:cat.name|lower}) }}">{{ cat.name }}</a> </li>
    {% endfor %}
</ul>